package concurency.chat.server;

import lombok.RequiredArgsConstructor;
import lombok.SneakyThrows;

@RequiredArgsConstructor
public class FileTransferHandler {

    /* private final ServerWorkers workers;

    @SneakyThrows
    public void sendFileRequest(Worker sender, String receiverName, String fileName) {
        // TODO - implement check if in the same room

        Worker receiver = workers.get(receiverName);
        if (receiver == null) {
            sender.sendText(String.format("File receiver %s does not exist", receiverName));
        }
    } */
}
